# 2026-03-06 — Daily Log

## LiteLLM Cost Logger — FIXED ✅
- **Root cause**: `success_callback` in LiteLLM YAML only works for built-in integrations (langfuse, etc.)
- **Fix**: Use `callbacks: cost_logger.proxy_handler_instance` (not `success_callback: ["cost_logger.cost_logger"]`)
- Custom callback class must extend `litellm.integrations.custom_logger.CustomLogger` and expose `proxy_handler_instance` at module level
- Both `log_success_event` and `async_log_success_event` must be implemented
- **PYTHONPATH** must include the directory containing `cost_logger.py` — solved via wrapper script `start.sh`
- Pricing dict needed both prefixed (`anthropic/claude-opus-4-6`) and unprefixed (`claude-opus-4-6`) keys since LiteLLM passes model names inconsistently
- **LaunchD plist trick**: Use a bash wrapper script (`start.sh`) that sets env vars + PYTHONPATH, then `exec` litellm — cleaner than trying to set PYTHONPATH in plist XML
- Table `litellm_usage_log` in `~/.local/share/people/people.db` now receives entries (18+ as of 08:30)
- Verified: Opus, Sonnet, Gemini Flash all log correctly with costs

## LiteLLM Service Config
- **Wrapper script**: `~/.openclaw/workspace/litellm-proxy/start.sh` (sets PYTHONPATH, API keys, exec litellm)
- **Plist**: `~/Library/LaunchAgents/com.openclaw.litellm.plist` — calls `start.sh` via `/bin/bash`
- **Config YAML key**: `litellm_settings.callbacks` (NOT `litellm_settings.success_callback`)
- 4 healthy models: claude-opus-4-6, claude-sonnet-4-6, gemini-flash, gemini-pro
- 1 unhealthy: claude-haiku-3-5 (naming issue, minor)

## Codex CLI Configured ✅
- `codex` v0.111.0 installed, model `gpt-5.3-codex`, auth via `~/.codex/auth.json`
- `claude` (Claude Code) v2.1.70 also available
- Added trusted projects to `~/.codex/config.toml`: people-crm, fieldy-webhook, litellm-proxy, ~/Dev
- Tested successfully on people-crm — Codex explored full project and described architecture
- **Usage pattern**: `codex exec --full-auto "prompt"` with `pty:true` in background for complex coding tasks
- Codex requires a git repo to run (use `mktemp -d && git init` for scratch)

## Sylvain's Morning Messages (context)
- 07:17 — "looks like we break our model usage when making the LiteLLM proxy change" → confirmed LiteLLM was down because I killed it yesterday without restarting
- 07:53 — Wants to use OpenAI Codex as a tool for Synapse
- 08:22 — "configurons codex" → done
- Cannot buy more Anthropic credits (then corrected: transaction went through)

## Maison — Notes
- **Abris Tandale** (à confirmer orthographe) — noté comme outil/option pour la maison à Irlande (à confirmer le nom de lieu)
- Sylvain était au **Salon du vélo de Gatineau** aujourd'hui — a vu un vélo Genesis avec moteurs Annex Sport (AS)

## Pending Items
- Dashboard `/dashboard/costs` needs update to read from `litellm_usage_log`
- CRM Activity/Insights tabs need fixing
- `qmd embed` still not run (1752+ hashes need vectors)
- Memory Guardian `run.sh` needs rework (call LiteLLM directly instead of openclaw CLI)
- Daily summary end-to-end test pending
- Saved views UI not yet in contacts template
- Codex as ACP agent in openclaw.json not yet configured
