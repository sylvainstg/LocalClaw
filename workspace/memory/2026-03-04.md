# 2026-03-04

## Fieldy Pipeline — Mémoire long terme ✅

Implémenté les 3 étapes du plan Fieldy → mémoire long terme :

### 1. Webhook amélioré (`server.py`)
- Parse `transcriptions[]` avec speakers A/B/C
- Écrit un markdown quotidien dans `~/.local/share/fieldy/transcriptions/YYYY-MM-DD.md`
- Format : header par jour, sections horodatées, speakers identifiés
- Trigger `qmd embed fieldy` en background après chaque write

### 2. Collection QMD `fieldy`
- Créée et indexée : pointe vers `~/.local/share/fieldy/transcriptions/`
- Pattern `**/*.md`, embeddings Gemma
- Cherchable via `qmd search "..." -c fieldy` ou `qmd query "..."`

### 3. Résumé quotidien 19h (`daily_summary.py`)
- Lit le markdown du jour (plus lisible que le brut SQLite)
- Tente un résumé structuré via `openclaw run` (thèmes, décisions, suivis)
- Fallback sur markdown brut si l'AI échoue
- Envoi Telegram à 19h (cron existant)

### Backfill
- Transcriptions existantes (2 mars, 3 mars) converties en markdown et indexées

### Corrections
- TOOLS.md : corrigé le chemin du People CRM (`~/.openclaw/workspace/people-crm/`)
- Note ajoutée : pas de commande `calendar` dans le CRM, events dans table `interactions`

### Fichiers modifiés
- `/Users/sylvain/Projects/fieldy-webhook/server.py`
- `/Users/sylvain/Projects/fieldy-webhook/daily_summary.py`

### Webhook restart
- Service `com.openclaw.fieldy-webhook` redémarré, PID confirmé

### Whisper turbo
- Modèle Whisper changé de `base` → `turbo` dans `openclaw.json` (tools.media.audio)
- Gateway redémarré
- **Leçon :** Toujours notifier Sylvain sur Telegram quand une tâche est complétée — il n'a pas eu de feedback sur ce changement
- La transcription auto des vocaux par OpenClaw ne semble pas fonctionner (hook silencieux?) — à investiguer

### Synapse subdomain
- `synapse.sylvainstg.cloud` → localhost:8000 (People CRM) — était déjà configuré dans Cloudflare tunnel
- Accessible depuis le téléphone de Sylvain

### Préférences vocales de Sylvain
- Veut pouvoir envoyer des vocaux comme directives (ça marche déjà via Whisper)
- Veut des mémos vocaux → mémoire (pipeline voix → transcription → QMD)
